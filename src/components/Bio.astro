---
// Home bio block: portrait (grayscale) beside name/role/blurb.
import { BIO } from '../consts';
---

<div class="bio">
	<div class="bio__mount">
		<img class="bio__photo" src={BIO.photo} width="170" height="170" alt={BIO.name} />
	</div>
	<div class="bio__body">
		<p class="bio__name">{BIO.name}</p>
		<p class="bio__role label">{BIO.role}</p>
		<p class="bio__location meta">{BIO.location}</p>
		<p class="bio__blurb">{BIO.blurb}</p>
	</div>
</div>

<style>
	.bio {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: var(--sp-6);
		align-items: start;
	}
	/* Portrait mount: a clean, modern offset keyline — a thin ink border with a
	   hairline outline floating just outside it (flat, no mat, no shadow). */
	.bio__mount {
		width: max-content;
	}
	.bio__photo {
		display: block;
		width: 170px;
		height: 170px;
		object-fit: cover;
		filter: grayscale(1) contrast(1.05);
		border: var(--rule-thin);
		outline: var(--rule-hair);
		outline-offset: var(--sp-2);
	}
	.bio__name {
		font-family: var(--font-display);
		font-size: var(--fs-h1);
		line-height: 1.1;
		margin: 0 0 var(--sp-2);
	}
	.bio__role {
		margin: 0;
	}
	.bio__location {
		margin: var(--sp-1) 0 var(--sp-3);
		padding-bottom: var(--sp-3);
		border-bottom: 3px double var(--ink);
	}
	.bio__blurb {
		margin: 0;
		max-width: var(--measure);
	}
	/* Drop cap opens the blurb, echoing the article-page treatment (§3). */
	.bio__blurb::first-letter {
		float: left;
		font-family: var(--font-display);
		font-size: 2.6em;
		line-height: 0.8;
		padding: 0.05em 0.1em 0 0;
		color: var(--accent);
	}
	@media (max-width: 34rem) {
		.bio {
			grid-template-columns: 1fr;
			justify-items: center;
			text-align: center;
		}
		.bio__body {
			text-align: left;
			width: 100%;
		}
	}
</style>
